/**
 * Encapsulates document topic information.
 */
export class DocumentTopic {
    constructor (topic) {
        this.topic = topic;
        this.isDocumented = false;
        this.isSelected = false;
    }
}
