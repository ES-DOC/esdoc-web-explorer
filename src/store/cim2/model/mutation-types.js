// Initialises state store - part of application setup process.
export const INITIALISE = 'initialise';

// Sets current document.
export const SET_DOCUMENT = 'setDocument';

// Sets current document topic.
export const SET_DOCUMENT_TOPIC = 'setDocumentTopic';

// Sets current institute.
export const SET_INSTITUTION = 'setInstitution';

// Sets flag indicating whether a document is being loaded into memory.
export const SET_IS_LOADING = 'setIsLoading';

// Sets set of sources.
export const SET_SOURCES = 'setSources';

// Sets current source.
export const SET_SOURCE = 'setSource';
